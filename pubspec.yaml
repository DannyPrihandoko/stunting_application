name: stunting_application
description: "A new Flutter project."
publish_to: 'none'  # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^2.27.0 # Pastikan versi ini adalah yang terbaru atau stabil
  firebase_database: ^10.4.0 # Pastikan versi ini adalah yang terbaru atau stabil

dev_dependencies:
  flutter_native_splash: ^2.4.6  # Update to latest compatible version
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0  # Update to the latest version
  lints: ^6.0.0  # Add or update this if needed

# Optional: Uncomment the assets section if you have any assets (e.g., images, fonts)
# flutter:
#   uses-material-design: true
#   assets:
#     - assets/splash_screen_image.png

# You can also add custom fonts if needed here as per your requirement.
# fonts:
#   - family: CustomFont
#     fonts:
#       - asset: fonts/CustomFont-Regular.ttf
#       - asset: fonts/CustomFont-Bold.ttf
#         weight: 700
